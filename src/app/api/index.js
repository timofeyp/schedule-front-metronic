import session from 'app/api/session';
import schedule from 'app/api/schedule';
import creating from 'app/api/creating';
import ldap from 'app/api/ldap';
import VCParts from 'app/api/v-c-parts';

export default {
  session,
  schedule,
  creating,
  ldap,
  VCParts,
};
